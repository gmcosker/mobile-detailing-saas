'use client'

// Stubbed install prompt: renders nothing (desktop and mobile).
// Keeps layout imports satisfied while ensuring no prompt is shown.
export default function InstallPrompt() {
  return null
}
